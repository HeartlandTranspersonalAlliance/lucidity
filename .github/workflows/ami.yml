name: Validate AMI compatibility

on:
  pull_request:
    paths:
      - .github/workflows/ami.yml
      - Containerfile
      - ci/Containerfile
      - ci/setup-build-cache.sh
      - ci/teardown-build-cache.sh
      - flake.lock
      - flake.nix
      - image/**
      - roles/**
      - scripts/build.sh
      - scripts/build-disk.sh
      - scripts/validate-ami-import.sh
      - scripts/validate-disk.sh
  workflow_dispatch:
    inputs:
      run_aws_validation:
        description: Upload the raw artifact, register a disposable AMI, and validate its AWS metadata
        required: true
        type: boolean
        default: false
      ami_lifecycle:
        description: Disposable validates and cleans up; retained publishes an immutable boot-validated worker AMI
        required: true
        type: choice
        options:
          - disposable
          - retained
        default: disposable
      run_aws_launch:
        description: Boot the disposable AMI on t3a.small and validate the guest through SSM
        required: true
        type: boolean
        default: false
      release_version:
        description: Optional immutable vX.Y.Z release associated with a retained AMI
        required: false
        type: string
        default: ""
      source_image_digest:
        description: Optional immutable worker OCI digest associated with the release
        required: false
        type: string
        default: ""
      sbom_sha256:
        description: Optional SHA-256 of the worker SPDX SBOM associated with the release
        required: false
        type: string
        default: ""

concurrency:
  group: ami-validation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  packages: write

env:
  AWS_REGION: us-east-2
  CI_TOOLS_IMAGE: localhost/coolify-aws-ci-tools:ami
  IMAGE: localhost/coolify-bootc-worker:ami-validation

jobs:
  worker-ami:
    runs-on: ubuntu-24.04
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Report runner capacity
        run: |
          df -h /
          docker system df
          sudo podman system df

      - name: Resolve worker image reference
        env:
          AMI_LIFECYCLE: ${{ inputs.ami_lifecycle }}
          ECR_REPOSITORY_URL: ${{ vars.AWS_ECR_WORKER_REPOSITORY_URL }}
          RELEASE_VERSION: ${{ inputs.release_version }}
        run: |
          if [[ "${AMI_LIFECYCLE}" == retained ]]; then
            [[ "${ECR_REPOSITORY_URL}" =~ ^[0-9]{12}\.dkr\.ecr\.[a-z0-9-]+\.amazonaws\.com/[a-z0-9]+([._/-][a-z0-9]+)*$ ]] || {
              echo "AWS_ECR_WORKER_REPOSITORY_URL must be configured for retained AMIs" >&2
              exit 1
            }
            if [[ -n "${RELEASE_VERSION}" ]]; then
              worker_image_ref="${ECR_REPOSITORY_URL}:${RELEASE_VERSION}"
            else
              worker_image_ref="${ECR_REPOSITORY_URL}:sha-${GITHUB_SHA}"
            fi
          else
            worker_image_ref="${IMAGE}"
          fi
          echo "WORKER_IMAGE_REF=${worker_image_ref}" >> "${GITHUB_ENV}"

      - name: Configure AWS credentials for retained ECR candidate
        if: github.event_name == 'workflow_dispatch' && inputs.ami_lifecycle == 'retained'
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          allowed-account-ids: 467590374785
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_AMI_IMPORT_ROLE_ARN }}
          role-duration-seconds: 10800
          role-session-name: lucidity-ami-source-${{ github.run_id }}

      - name: Pull immutable ECR worker candidate
        if: github.event_name == 'workflow_dispatch' && inputs.ami_lifecycle == 'retained'
        env:
          SOURCE_IMAGE_DIGEST: ${{ inputs.source_image_digest }}
        run: |
          registry=${WORKER_IMAGE_REF%%/*}
          image_without_tag=${WORKER_IMAGE_REF%:*}
          repository_name=${image_without_tag#*/}
          source_tag=${WORKER_IMAGE_REF##*:}
          resolved_digest=$(aws ecr batch-get-image \
            --region "${AWS_REGION}" \
            --repository-name "${repository_name}" \
            --image-ids "imageTag=${source_tag}" \
            --query 'images[0].imageId.imageDigest' \
            --output text)
          [[ ${resolved_digest} =~ ^sha256:[0-9a-f]{64}$ ]] || { echo "retained worker candidate did not resolve to an OCI digest" >&2; exit 1; }
          if [[ -n "${SOURCE_IMAGE_DIGEST}" && "${resolved_digest}" != "${SOURCE_IMAGE_DIGEST}" ]]; then
            echo "retained worker candidate digest does not match the release manifest input" >&2
            exit 1
          fi
          aws ecr get-login-password --region "${AWS_REGION}" | sudo podman login --username AWS --password-stdin "${registry}"
          sudo podman pull "${WORKER_IMAGE_REF}"
          sudo env CONTAINER_ENGINE=podman ./scripts/validate-image.sh "${WORKER_IMAGE_REF}"
          [[ $(sudo podman image inspect --format '{{.Os}}/{{.Architecture}}' "${WORKER_IMAGE_REF}") == linux/amd64 ]] || {
            echo "retained AMI source is not linux/amd64" >&2
            exit 1
          }

      - name: Build pinned image tooling
        env:
          AMI_LIFECYCLE: ${{ inputs.ami_lifecycle }}
        run: |
          source image/image-builder.env
          if [[ "${AMI_LIFECYCLE}" == retained ]]; then
            sudo podman build \
              --build-arg "IMAGE_BUILDER_IMAGE=${IMAGE_BUILDER_IMAGE}" \
              --tag "${CI_TOOLS_IMAGE}" \
              --file ci/Containerfile \
              .
          else
            docker build \
              --build-arg "IMAGE_BUILDER_IMAGE=${IMAGE_BUILDER_IMAGE}" \
              --tag "${CI_TOOLS_IMAGE}" \
              --file ci/Containerfile \
              .
          fi

      - name: Prepare role-scoped GHCR build cache
        if: github.event_name != 'workflow_dispatch' || inputs.ami_lifecycle != 'retained'
        env:
          GHCR_CACHE_WRITE: ${{ github.event_name != 'pull_request' }}
          GITHUB_TOKEN: ${{ github.token }}
        run: ci/setup-build-cache.sh worker

      - name: Build AMD64 worker bootc image
        if: github.event_name != 'workflow_dispatch' || inputs.ami_lifecycle != 'retained'
        env:
          ARCH: amd64
          CONTAINER_ENGINE: docker
          IMAGE_NAME: ${{ env.WORKER_IMAGE_REF }}
          IMAGE_VERSION: ami-validation
        run: ./scripts/build.sh worker

      - name: Build and validate raw AMI artifact
        env:
          AMI_LIFECYCLE: ${{ inputs.ami_lifecycle }}
        run: |
          if [[ "${AMI_LIFECYCLE}" == retained ]]; then
            sudo env \
              CONTAINER_ENGINE=podman \
              IMAGE_NAME="${WORKER_IMAGE_REF}" \
              VM_TOOL_IMAGE="${CI_TOOLS_IMAGE}" \
              ./scripts/build-disk.sh worker ami
            sudo env \
              CONTAINER_ENGINE=podman \
              VM_TOOL_IMAGE="${CI_TOOLS_IMAGE}" \
              ./scripts/validate-disk.sh image-output/worker/coolify-worker-ami.raw
          else
            CONTAINER_ENGINE=docker \
              IMAGE_NAME="${WORKER_IMAGE_REF}" \
              VM_TOOL_IMAGE="${CI_TOOLS_IMAGE}" \
              ./scripts/build-disk.sh worker ami
            CONTAINER_ENGINE=docker \
              VM_TOOL_IMAGE="${CI_TOOLS_IMAGE}" \
              ./scripts/validate-disk.sh image-output/worker/coolify-worker-ami.raw
          fi

      - name: Require configured AWS validation identifiers
        if: github.event_name == 'workflow_dispatch'
        env:
          RUN_AWS_VALIDATION: ${{ inputs.run_aws_validation }}
          RUN_AWS_LAUNCH: ${{ inputs.run_aws_launch }}
          AMI_LIFECYCLE: ${{ inputs.ami_lifecycle }}
          IMPORT_ROLE_ARN: ${{ vars.AWS_AMI_IMPORT_ROLE_ARN }}
          SNAPSHOT_KMS_KEY_ARN: ${{ vars.AWS_AMI_SNAPSHOT_KMS_KEY_ARN }}
          ECR_WORKER_REPOSITORY_URL: ${{ vars.AWS_ECR_WORKER_REPOSITORY_URL }}
          RELEASE_VERSION: ${{ inputs.release_version }}
          SOURCE_IMAGE_DIGEST: ${{ inputs.source_image_digest }}
          SBOM_SHA256: ${{ inputs.sbom_sha256 }}
          TEST_INSTANCE_PROFILE: ${{ vars.AWS_AMI_TEST_INSTANCE_PROFILE_NAME }}
          TEST_SECURITY_GROUP: ${{ vars.AWS_AMI_TEST_SECURITY_GROUP_ID }}
          TEST_SUBNET: ${{ vars.AWS_AMI_TEST_SUBNET_ID }}
        run: |
          if [[ "${RUN_AWS_LAUNCH}" == true && "${RUN_AWS_VALIDATION}" != true ]]; then
            echo "run_aws_launch requires run_aws_validation" >&2
            exit 1
          fi
          if [[ "${AMI_LIFECYCLE}" == retained ]]; then
            [[ "${RUN_AWS_VALIDATION}" == true ]] || { echo "retained AMI publication requires run_aws_validation" >&2; exit 1; }
            [[ "${RUN_AWS_LAUNCH}" == true ]] || { echo "retained AMI publication requires the disposable EC2 launch gate" >&2; exit 1; }
            [[ "${GITHUB_REF}" == refs/heads/main ]] || { echo "retained AMIs may be published only from main" >&2; exit 1; }
            test -n "${ECR_WORKER_REPOSITORY_URL}" || { echo "AWS_ECR_WORKER_REPOSITORY_URL is required for retained AMIs" >&2; exit 1; }
          fi
          if [[ -n "${RELEASE_VERSION}" || -n "${SOURCE_IMAGE_DIGEST}" || -n "${SBOM_SHA256}" ]]; then
            [[ "${AMI_LIFECYCLE}" == retained ]] || { echo "release metadata is restricted to retained AMIs" >&2; exit 1; }
            [[ "${RELEASE_VERSION}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]] || { echo "release_version must be v-prefixed SemVer" >&2; exit 1; }
            [[ "${SOURCE_IMAGE_DIGEST}" =~ ^sha256:[0-9a-f]{64}$ ]] || { echo "source_image_digest must be a lowercase OCI SHA-256 digest" >&2; exit 1; }
            [[ "${SBOM_SHA256}" =~ ^[0-9a-f]{64}$ ]] || { echo "sbom_sha256 must be a lowercase SHA-256 digest" >&2; exit 1; }
          fi
          [[ "${RUN_AWS_VALIDATION}" == true ]] || exit 0
          test -n "${IMPORT_ROLE_ARN}" || { echo "AWS_AMI_IMPORT_ROLE_ARN repository variable is required" >&2; exit 1; }
          test -n "${SNAPSHOT_KMS_KEY_ARN}" || { echo "AWS_AMI_SNAPSHOT_KMS_KEY_ARN repository variable is required" >&2; exit 1; }
          if [[ "${RUN_AWS_LAUNCH}" == true ]]; then
            test -n "${TEST_INSTANCE_PROFILE}" || { echo "AWS_AMI_TEST_INSTANCE_PROFILE_NAME repository variable is required" >&2; exit 1; }
            test -n "${TEST_SECURITY_GROUP}" || { echo "AWS_AMI_TEST_SECURITY_GROUP_ID repository variable is required" >&2; exit 1; }
            test -n "${TEST_SUBNET}" || { echo "AWS_AMI_TEST_SUBNET_ID repository variable is required" >&2; exit 1; }
          fi

      - name: Install Determinate Nix for the pinned EBS uploader
        if: github.event_name == 'workflow_dispatch' && inputs.run_aws_validation
        uses: DeterminateSystems/determinate-nix-action@61cbfe2efc2d4e7a8a6d56967c3c1058e846c858 # v3.21.9

      - name: Resolve the pinned coldsnap binary
        if: github.event_name == 'workflow_dispatch' && inputs.run_aws_validation
        run: |
          coldsnap_path=$(nix build --no-link --print-out-paths .#coldsnap)
          test -x "${coldsnap_path}/bin/coldsnap"
          echo "COLDSNAP_COMMAND=${coldsnap_path}/bin/coldsnap" >> "${GITHUB_ENV}"

      - name: Configure short-lived AWS credentials
        if: github.event_name == 'workflow_dispatch' && inputs.run_aws_validation
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          allowed-account-ids: 467590374785
          aws-region: ${{ env.AWS_REGION }}
          role-to-assume: ${{ vars.AWS_AMI_IMPORT_ROLE_ARN }}
          role-duration-seconds: 10800
          role-session-name: lucidity-ami-${{ github.run_id }}

      - name: Validate EBS Direct snapshot and AMI
        if: github.event_name == 'workflow_dispatch' && inputs.run_aws_validation
        env:
          AMI_EXPECTED_BOOTC_IMAGE_REF: ${{ env.WORKER_IMAGE_REF }}
          AMI_LIFECYCLE: ${{ inputs.ami_lifecycle }}
          AMI_RELEASE_VERSION: ${{ inputs.release_version }}
          AMI_ROLE: worker
          AMI_SBOM_SHA256: ${{ inputs.sbom_sha256 }}
          AMI_SOURCE_IMAGE_DIGEST: ${{ inputs.source_image_digest }}
          AMI_SOURCE_REVISION: ${{ github.sha }}
          AMI_LAUNCH_VALIDATION: ${{ inputs.run_aws_launch }}
          AMI_SNAPSHOT_KMS_KEY_ARN: ${{ vars.AWS_AMI_SNAPSHOT_KMS_KEY_ARN }}
          AMI_TEST_INSTANCE_PROFILE_NAME: ${{ vars.AWS_AMI_TEST_INSTANCE_PROFILE_NAME }}
          AMI_TEST_INSTANCE_TYPE: t3a.small
          AMI_TEST_SECURITY_GROUP_ID: ${{ vars.AWS_AMI_TEST_SECURITY_GROUP_ID }}
          AMI_TEST_SUBNET_ID: ${{ vars.AWS_AMI_TEST_SUBNET_ID }}
        run: ./scripts/validate-ami-import.sh image-output/worker/coolify-worker-ami.raw

      - name: Remove retained ECR login
        if: always() && github.event_name == 'workflow_dispatch' && inputs.ami_lifecycle == 'retained'
        run: sudo podman logout "${WORKER_IMAGE_REF%%/*}"

      - name: Remove GHCR login and BuildKit builder
        if: always()
        run: ci/teardown-build-cache.sh
